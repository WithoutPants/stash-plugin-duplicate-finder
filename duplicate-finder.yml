name: Duplicate File Finder
description: Finds perceptually duplicate scenes using sprite files.
version: 0.1.5
url: https://github.com/WithoutPants/stash-plugin-duplicate-finder
exec:
  - plugin_duplicate_finder
interface: rpc
tasks:
  - name: Find duplicate scenes
    description: Finds perceptually duplicate scenes
      